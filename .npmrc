# Remove once typescript-eslint supports TypeScript 6
legacy-peer-deps=true
# Don't install packages released less than 7 days ago, to reduce risk of
# security issues. npm (as of 11.12.1) has no per-package exclusion; to
# override for a single command, pass `--min-release-age=0` on the CLI.
min-release-age=7
